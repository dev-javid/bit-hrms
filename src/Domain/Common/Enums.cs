namespace Domain.Common
{
    public class Enums
    {
        public enum RoleName
        {
            SuperAdmin,
            CompanyAdmin,
            Employee
        }

        public enum DocumentType
        {
            PAN,
            Aadhar
        }

        public enum DeductionType
        {
            Absent,
        }
    }
}
